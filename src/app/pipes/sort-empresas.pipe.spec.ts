import { SortEmpresasPipe } from './sort-empresas.pipe';

describe('SortEmpresasPipe', () => {
  it('create an instance', () => {
    const pipe = new SortEmpresasPipe();
    expect(pipe).toBeTruthy();
  });
});
