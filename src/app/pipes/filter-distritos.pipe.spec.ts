import { FilterDistritosPipe } from './filter-distritos.pipe';

describe('FilterDistritosPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterDistritosPipe();
    expect(pipe).toBeTruthy();
  });
});
