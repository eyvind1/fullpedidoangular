import { FilterComboBoxPipe } from './filter-combo-box.pipe';


describe('FilterComboBoxPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterComboBoxPipe();
    expect(pipe).toBeTruthy();
  });
});
