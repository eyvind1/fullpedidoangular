import { FilterProvinciasPipe } from './filter-provincias.pipe';

describe('FilterProvinciasPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterProvinciasPipe();
    expect(pipe).toBeTruthy();
  });
});
