import { FilterCategoriasCarruselPipe } from './filter-categorias-carrusel.pipe';

describe('FilterCategoriasCarruselPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterCategoriasCarruselPipe();
    expect(pipe).toBeTruthy();
  });
});
